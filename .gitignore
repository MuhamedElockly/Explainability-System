# Secrets — never commit API keys
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/

# Local conda copy (should not live in repo)
.conda/

# Generated reports (regenerate with test_model.py)
incident_reports/*.pdf

# Generated attack-flow sample CSV (run export_attack_sample.py)
sample_datasets/*.csv

# Chroma vector store (rebuilt on first run from packaged corpus)
rag_chroma/

# Jupyter — notebooks can embed API keys; exclude by default (copy snippets into docs if needed)
*.ipynb
.ipynb_checkpoints/

# Tools
.pytest_cache/
.mypy_cache/
*.log

# OS
.Desktop.ini
Thumbs.db
